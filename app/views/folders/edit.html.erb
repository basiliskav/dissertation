<%= form_for @folder do |f| %>
  <%= f.label :name %> <br />
  <%= f.text_field :name, value: @folder.name %>
  <% f.text_field :user_id, :value => current_user.id %>
  <%= f.submit "Update it !" %>
<% end %>